{
  "deploy": {
    "startCommand": "sh -c 'npx prisma db push --accept-data-loss && echo \"=== Prisma OK — iniciando NestJS ===\" && node dist/main'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

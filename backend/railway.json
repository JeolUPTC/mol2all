{
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && echo '--- dist contents ---' && ls dist/ && echo '--- starting node ---' && node -r tsconfig-paths/register dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

{
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && node -r tsconfig-paths/register dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

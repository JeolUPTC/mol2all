{
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node -r tsconfig-paths/register dist/main",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
